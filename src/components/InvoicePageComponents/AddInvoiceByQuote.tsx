import { Button } from "@chakra-ui/react";
import React from "react";

const AddInvoiceByQuote = () => {
  return <Button>Add Invoice with pre-existing quote</Button>;
};

export default AddInvoiceByQuote;
